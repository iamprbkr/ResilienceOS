{
  "name": "ai-native-cyber-resilience-platform",
  "version": "0.1.0",
  "private": true,
  "description": "AI-native cyber resilience and governance simulation SaaS scaffold.",
  "workspaces": [
    "apps/frontend",
    "apps/api"
  ],
  "scripts": {
    "dev": "pnpm --parallel -r run dev",
    "start": "node apps/api/dist/server.js",
    "build": "pnpm --filter frontend build && pnpm --filter api build",
    "db:migrate": "pnpm --filter api db:migrate",
    "selftest": "pnpm --filter api selftest",
    "smoke": "pnpm --filter api smoke",
    "lint": "pnpm --filter frontend lint && pnpm --filter api lint",
    "typecheck": "pnpm --filter frontend typecheck && pnpm --filter api typecheck"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  },
  "allowScripts": {}
}
